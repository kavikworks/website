# Kavik Works agent instructions

Read `docs/operating-architecture.md`, `docs/business-desk.md`, and the current private Notion Operations page before business work. The September 2026 low-maintenance model supersedes older volume-first instructions in the wiki and unmerged architecture proposals. An explicit newer owner instruction takes precedence.

Keep this public repository free of customer details, private workspace URLs, email message IDs, credentials, and payment data. Current operational facts belong in the private Notion operations board. Gmail is the evidence for sent mail; a spreadsheet row is not proof of sending.

The website is static HTML/CSS/JavaScript on the existing hosting arrangement. Preserve established public URLs. Do not create another backend, host, CRM, or recurring task to solve a routine issue. Legacy backend files under `automations/` are retained for history; their live deployment is unverified and the new intake does not call them. Do not redeploy them without separately verified runtime authority and configuration.

The public offer is a $149 one-time written Workflow Blueprint for one intake or follow-up workflow. Scope, any applicable tax, delivery timing, and payment method must be confirmed before payment. Software installation and monitoring are separate. Never invent clients, results, testimonials, credentials, available checkout links, appointments, or implementation capabilities. Current scope excludes regulated decisions and emergency dispatch.

For website changes, run `python3 scripts/check_site.py` and `node --test tests/workflow-check.test.cjs`. Keep the visible offer, structured data, examples, and llms.txt consistent. Routine factual improvements within the owner's authorized scope may be published after checks using a reviewable PR and ordinary merge protections; record commit and live verification. Do not equate merging with deployment. Do not change prices, financial terms, payments, tracking, hosting, or security configuration as a routine growth experiment.

Business agents must verify the connected sender and full thread, record intent before a side effect, and reconcile uncertain completion before retrying. No new cold sequences during migration. Honor replies, opt-outs, bounces, existing drafts, and later human messages. Only the Business Desk may send routine business replies. No personal inbox cleanup under this mandate.

Use existing connected tools. Do not repeatedly request Google Apps Script domain access for this project. Record one actionable blocker when a required capability is absent. Do not manufacture data or instructions to bypass it.
